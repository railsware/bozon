const cyan = jest.fn(value => value)
const yellow = jest.fn(value => value)
const red = jest.fn(value => value)
const green = jest.fn(value => value)
const bold = jest.fn(value => value)

export default { cyan, yellow, red, green, bold }
